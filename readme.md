# Java Web W/O Framework

A project using no web frameworks in java to do a REST API backend.

Endpoints:

- `/api/`: GET, POST | returns "Hello World!" or "Foi Post!" if its a POST request

To start, properly install an java 21+, and execute it using the following command:

`javac src/Main.java && java -cp src Main`
